#Delete signs from str

for i in df1.columns:
    df1[i] = df1[i].replace('[,\n]', '', regex = True)
    
    
   if analysis.sentiment.polarity > 0:
        po = po + 1
    elif analysis.sentiment.polarity == 0:
        ne = ne + 1
    else:
        neg = neg + 1
		
